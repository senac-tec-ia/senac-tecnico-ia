# Perguntas por Papel — Av.06 Seminário de Funções

1 pergunta por papel por grupo. Total: 30 perguntas.

Papéis: **Formula · Dados · Calculo · Grafico · Mundo real**

---

## Grupo 1 — Função Linear `y = ax + b`

| Papel | Pergunta |
|---|---|
| Formula | No codigo de vocês, quais sao os valores de `a` e `b`? O que acontece com o grafico se voce dobrar `a`? |
| Dados | Por que o `np.linspace` comeca e termina nos valores que vocês escolheram? O que mudaria se o intervalo fosse menor? |
| Calculo | A linha `y = a*x + b` — o que o Python esta fazendo com cada valor de x? Por que o resultado e sempre uma reta? |
| Grafico | No grafico de vocês, o que o eixo x representa e o que o eixo y representa? |
| Mundo real | Qual numero concreto vocês usaram no exemplo? Onde ele aparece no codigo? |

---

## Grupo 2 — Função Quadrática `y = ax² + bx + c`

| Papel | Pergunta |
|---|---|
| Formula | O que muda no formato da parabola quando voce altera `a`? E quando altera `c`? |
| Dados | Por que vocês escolheram esse intervalo no `np.linspace`? O intervalo foi pensado para mostrar o vertice? |
| Calculo | O que o `**2` faz no Python? Por que isso faz a curva dobrar em vez de continuar reta? |
| Grafico | Onde esta o vertice no grafico de vocês? Como o grafico deixa isso visivel? |
| Mundo real | Por que uma parabola modela melhor a situacao que vocês escolheram do que uma reta? |

---

## Grupo 3 — Função Polinomial grau 3 `y = ax³ + bx² + cx + d`

| Papel | Pergunta |
|---|---|
| Formula | O que muda no grafico quando voce altera o valor de `a`? E quando altera `d`? |
| Dados | Por que o intervalo que vocês escolheram no `np.linspace` e bom para mostrar as duas curvas do grau 3? |
| Calculo | O que o `x**3` faz de diferente do `x**2`? Por que o grau 3 faz a curva mudar de direcao mais de uma vez? |
| Grafico | O grafico de grau 3 tem mais de uma direcao — onde isso aparece na curva de vocês? |
| Mundo real | Qual dado concreto vocês usaram e como o codigo foi alterado para refletir esse exemplo? |

---

## Grupo 4 — Função Logarítmica `y = log(x)`

| Papel | Pergunta |
|---|---|
| Formula | Por que o dominio da funcao logaritmica exige que x seja maior que zero? O que aconteceria matematicamente se x fosse 0? |
| Dados | Por que o `np.linspace` de vocês comeca em 0.1 e nao em 0? O que acontece se voce passar 0 para `np.log`? |
| Calculo | O que `np.log(x)` calcula exatamente? E o log na base 10 ou em outra base? |
| Grafico | O grafico da logaritmica desacelera conforme x cresce — onde isso aparece visualmente na curva de vocês? |
| Mundo real | Qual situacao real vocês escolheram e como vocês modificaram o codigo para mostrar o exemplo? |

---

## Grupo 5 — Função Exponencial `y = a · eˣ`

| Papel | Pergunta |
|---|---|
| Formula | Qual e o valor de `a` que vocês usaram? O que muda no grafico quando esse valor aumenta? |
| Dados | O crescimento exponencial sobe rapido — por que vocês escolheram esse intervalo no `np.linspace` e nao um maior? |
| Calculo | Qual e a diferenca entre `np.exp(x)` e escrever `2**x`? Por que usamos `e` como base e nao 2 ou 10? |
| Grafico | Em que ponto da curva de vocës o crescimento comeca a subir muito rapido? Como isso aparece visualmente? |
| Mundo real | Qual numero concreto vocës usaram no exemplo e onde ele aparece no codigo? |

---

## Grupo 6 — Função Sigmoide `y = 1 / (1 + e⁻ˣ)`

| Papel | Pergunta |
|---|---|
| Formula | Qual e o valor maximo e o minimo que a sigmoide pode produzir? Por que isso acontece matematicamente? |
| Dados | Por que o intervalo do `np.linspace` de vocës fica em torno do zero? O que acontece com o formato "S" se voce usar so de -1 a 1? |
| Calculo | O que `np.exp(-x)` calcula? Por que tem `-x` (negativo) e nao so `x`? |
| Grafico | O grafico da sigmoide tem formato de "S" — o que esse formato significa para a saida da funcao? |
| Mundo real | Vocës disseram que a sigmoide aparece num neuronio artificial — qual numero concreto vocës usaram para mostrar isso no codigo? |
